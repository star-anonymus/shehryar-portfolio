# Public URL of the deployed site, no trailing slash.
# Drives metadataBase, the sitemap, every Open Graph image and every
# LinkedIn share link.
#
# On Vercel you can leave this unset — it falls back to the project's
# production URL. Set it once a custom domain is live.
NEXT_PUBLIC_SITE_URL=
